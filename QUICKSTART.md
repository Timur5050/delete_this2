# 🚀 QUICKSTART GUIDE - Linux Process Viewer

## ⚡ Один раз при першому запуску

```bash
# Перейти в папку проекту
cd course

# Terminal 1: Встановити залежності Backend
cd backend
npm install

# Terminal 2 (новий терміналу): Встановити залежності Frontend
cd frontend
npm install
```

## 🎯 Запуск застосунку

### Terminal 1 - Backend Server
```bash
cd /home/timur/Documents/course/backend
npm run dev
```

Очікуйте вивід:
```
Server is running on http://localhost:5000
```

### Terminal 2 - Frontend App
```bash
cd /home/timur/Documents/course/frontend
npm start
```

Очікуйте:
```
You can now view linux-process-viewer-frontend in the browser.
Local: http://localhost:3000
```

## 🌐 Доступ до застосунку

**Frontend UI**: http://localhost:3000
**Backend API**: http://localhost:5000

## 🧪 Тестування API

```bash
# Перевірити здоров'я сервера
curl http://localhost:5000/api/health

# Отримати список всіх процесів
curl http://localhost:5000/api/processes

# Отримати кількість процесів
curl http://localhost:5000/api/processes | jq 'length'

# Отримати системну статистику
curl http://localhost:5000/api/stats

# Отримати конкретний процес (PID=1)
curl http://localhost:5000/api/processes/1

# Отримати всі снимки (пусто на початку)
curl http://localhost:5000/api/snapshots
```

## 📌 Функціональність UI

✅ **Пошук** - Введіть назву процесу, PID або користувача
✅ **Сортування** - Клікніть по заголовкам колон
✅ **CPU/RAM** - Кольорова підсвітка для розпізнання критичних процесів
✅ **Завершити процес** - Клікніть на червону кнопку (потребує прав)
✅ **Auto-refresh** - Налаштуйте інтервал оновлення (за замовчуванням 5 сек)

## 🛠 Розробка

### Структура файлів
```
course/
├── backend/
│   ├── server.js          # Основний сервер
│   ├── db.js              # Файлове сховище (JSON)
│   ├── data/              # JSON файли із даними
│   └── node_modules/      # Залежності
├── frontend/
│   ├── src/
│   │   ├── App.js         # Головний компонент
│   │   ├── components/    # React компоненти
│   │   └── App.css        # Стилі
│   └── public/
│       └── index.html     # HTML шаблон
└── docs/
    ├── COURSEWORK.md      # Курсова робота (описання)
    └── README.md          # Повна документація
```

### Редагування кода
- **Frontend код**: `frontend/src/`
- **Backend код**: `backend/server.js`
- **Стилі**: `frontend/src/components/*.css`

Зміни автоматично перезавантажуватися завдяки:
- Nodemon (backend)
- React dev server (frontend)

## ⚙️ Конфігурація

### Backend `.env`
```
PORT=5000
NODE_ENV=development
```

### Frontend API URL
Редагуйте в `frontend/src/App.js`:
```javascript
const API_URL = 'http://localhost:5000/api';
```

## 🔧 Розширення функціональності

### Додавання нового API endpoint
1. Редагуйте `backend/server.js`
2. Додайте новий route
3. Backend автоматично перезавантажиться

### Додавання нового компонента
1. Створіть файл в `frontend/src/components/`
2. Імпортуйте в `frontend/src/App.js`
3. Сторінка автоматично оновиться

## ❌ Розв'язування проблем

### "Cannot GET /"
- Впевніться, що frontend запущено на порту 3000
- Перезагрузіть браузер (Ctrl+R)

### "API unavailable"
- Перевірте, що backend запущено на порту 5000
- Запустіть: `curl http://localhost:5000/api/health`

### "Permission denied" при killing процесу
- Деякі процеси потребують root прав
- Запустіть терміналу з `sudo` або використайте правильні права доступу

### npm install не працює
- Видаліть `node_modules`: `rm -rf node_modules`
- Видаліть lock-файл: `rm package-lock.json`
- Запустіть: `npm install` заново

## 📚 Посилання

- [React Docs](https://react.dev)
- [Express Docs](https://expressjs.com)
- [Linux ps command](https://man7.org/linux/man-pages/man1/ps.1.html)

## 📝 Примітки

- Зберігання даних - JSON файли в `backend/data/`
- Не потребує БД для запуску
- Для production використовуйте PostgreSQL або MongoDB

---

**Happy coding! 🎉**
